<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ToDoProject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorOnPrimary">@color/white</item>
       <item name="android:windowActivityTransitions">true</item>
        <item name="android:windowEnterTransition">@transition/slide_trans</item>

        <item name="android:windowExitTransition">@transition/slide_trans</item>


    </style>

    <style name="Theme.ToDoProject" parent="Base.Theme.ToDoProject" />
</resources>